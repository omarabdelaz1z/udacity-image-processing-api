// Paths
export const FULL_IMAGE_PATH = "assets/images/full";
export const THUMB_IMAGE_PATH = "assets/images/thumb";

// Joi Error Messages
export const POSITIVE_NUMBER = "{{#label}} must be greater than zero";
export const REQUIRED_FIELD = "{{#label}} must be provided";

// Joi Validate Options
export const VALIDATE_OPTIONS = {
  errors: { wrap: { label: "" } },
};
